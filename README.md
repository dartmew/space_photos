# Space Photos & Bot

Набор скриптов для получения космических фотографий от NASA и SpaceX с возможностью отправки через Telegram-бота.

## 🌟 Что делает этот проект?

- **Скрипт `apodnasa.py`** - получает "Фото дня" от NASA (Astronomy Picture of the Day)
- **Скрипт `spacex.py`** - загружает фотографии последних запусков SpaceX
- **Скрипт `sibot.py`** - Telegram-бот, который автоматически отправляет космические фото в чат/канал
- **Все фото автоматически сохраняются** в папку `downloaded_images` для избежания повторных загрузок

## 🔗 Используемые сервисы

1. **NASA API** (api.nasa.gov) - для получения астрономических фото дня
2. **SpaceX API** - для получения фото запусков ракет
3. **Telegram Bot API** - для отправки фото через бота

## 🚀 Быстрый старт

### Шаг 1: Клонируйте репозиторий
```bash
git clone https://github.com/dartmew/space_photos.git
cd space_photos
```
### Шаг 2: Установите зависимости одной командой
```bash
pip install -r requirements.txt
```
### Шаг 3: Получите API ключи
Для NASA:
* Перейдите на api.nasa.gov

* Нажмите `Get Started` и заполните форму

* Получите ваш API ключ (пример: `DEMO_KEY` для теста, но лучше свой)

Для Telegram бота:
* Откройте Telegram и найдите `@BotFather`

* Отправьте `/newbot` и следуйте инструкциям

* Скопируйте токен бота (пример: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

* Создайте файл настроек бота:
```bash
# Создайте и откройте файл для редактирования
nano bot_settings.json
```
Вставьте следующий код, заменив значения на свои:
```json
{
    "images_folder": "images",
    "allowed_extensions": [".jpg", ".jpeg", ".png"],
    "caption_template": "🚀 Космическое фото 🌌"
}
```
Как получить `chat_id`:

* Добавьте бота в чат/канал

* Отправьте любое сообщение в чат

* Перейдите в браузере по ссылке: https://api.telegram.org/botВАШ_ТОКЕН/getUpdates

* Найдите `"chat":{"id":-1001234567890}` - это и есть ваш `chat_id`

### Шаг 4: Создайте файл `.env`:
```
NASA_TOKEN='ваш токен'
DEFAULT_NASA_TOKEN='DEMO_KEY'
TG_BOT_TOKEN='токен вашего бота'
TG_CHAT_ID='ID чата'
```

### Шаг 5: Протестируйте скрипты
Тест NASA APOD:
```bash
python apodnasa.py
Ожидаемый результат: В папке downloaded_images появится файл apod_2024-*.jpg
```

Тест SpaceX:
```bash
python spacex.py
Ожидаемый результат: В папке downloaded_images появится файл со случайным фото SpaceX
```

Тест бота:
* Запустите бота:
```bash
python sibot.py
```
* В Telegram найдите вашего бота по имени

* Отправьте команду `/start`

* Ожидаемый результат: Бот ответит приветствием

### Шаг 6: Проверьте результат
```bash
# Посмотрите загруженные фото
ls images/
```
## 📁 Структура файлов
```text
space_photos/
├── apodnasa.py          # Получает фото дня от NASA
├── spacex.py            # Получает фото запусков SpaceX
├── sibot.py             # Telegram-бот для отправки фото
├── utils.py             # Общие функции
├── bot_settings.json    # Настройки бота (вы создаете этот файл)
├── requirements.txt     # Список библиотек Python
├── .gitignore           # Какие файлы игнорировать в Git
└── images/              # Сюда сохраняются все фото
```
## ⚙️ Настройка времени отправки
Измените время в файле `bot_settings.json`:

```json
{
  "posting_time": "15:30"  # Формат ЧЧ:ММ, 24-часовой
}
```
#### 🔧 Что делать если не работает?
Проблема: `ModuleNotFoundError`
Решение: Повторите установку зависимостей:
```bash
pip install -r requirements.txt
```
Проблема: NASA API возвращает ошибку
Решение: Проверьте ключ в `.env`

Проблема: Бот не запускается
Решение: Проверьте токен и `chat_id`:

```json
{
  "token": "ПРОВЕРЬТЕ_ТОКЕН",
  "chat_id": "ПРОВЕРЬТЕ_CHAT_ID"
}
```
Проблема: Нет папки `images`
Решение: Запустите любой скрипт - папка создастся автоматически

## 📝 Примеры использования
Разовое получение фото дня:
```bash
python apodnasa.py
```
Запуск бота в фоновом режиме (Linux/Mac):
```bash
nohup python sibot.py > bot_output.log 2>&1 &
```