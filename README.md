# Space Photos Telegram Bot
### Requirements
```txt
python-dotenv==1.2.1
python-telegram-bot==13.15
requests==2.32.5
urllib3==2.6.3
```
### Структура проекта
```
├── sibot.py              # Основной Telegram бот
├── apodnasa.py           # Загрузчик APOD от NASA
├── spacex.py             # Загрузчик фото SpaceX
├── utils.py              # Вспомогательные функции
├── bot_settings.json     # Настройки бота
├── .env                  # Секретные ключи (создается вручную)
└── images/               # Папка для хранения изображений
```
### Использование
#### Настройка конфигурации
Отредактируйте bot_settings.json при необходимости:
```json
{
    "images_folder": "images",                          # Папка для изображений
    "allowed_extensions": [".jpg", ".jpeg", ".png"],    # Разрешенные форматы
    "caption_template": "🚀 Космическое фото 🌌",      # Подпись к фото
}
```
#### Загрузка изображений
Загрузка фото дня от NASA (APOD):
```bash
python apodnasa.py [количество]
```
##### Примеры:
```bash
python apodnasa.py           # Загрузит 5 фото (по умолчанию)
python apodnasa.py 10        # Загрузит 10 фото
```
##### Загрузка фото запусков SpaceX:
```bash
python spacex.py [id_запуска]
```
##### Примеры:
```bash
python spacex.py            # Загрузит фото последнего запуска
python spacex.py 5eb87d47ffd86e000604b38a  # Загрузит фото конкретного запуска
```
#### Запуск бота для публикации
##### Публикация всех изображений:
```bash
python sibot.py [интервал_в_секундах]
```
##### Примеры:
```bash
python sibot.py           # Публикация с интервалом 4 часа
python sibot.py 3600     # Публикация с интервалом 1 час
```
##### Публикация одного изображения:
```bash
python sibot.py имя_файла.jpg
```